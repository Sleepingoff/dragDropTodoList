import { todos, categories } from "./data.js";

import {
  generateId,
  generateTimestamp,
  checkDefaultCategory,
} from "./store.js";

export default {
  todos,
  categories,
  // Add other data objects as needed
  generateId,
  generateTimestamp,

  checkDefaultCategory,
};
